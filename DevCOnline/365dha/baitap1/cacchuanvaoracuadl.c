#include<stdio.h>

 int main (){
 	char c; //%c, mot ki tu
 	short s; //%hi, kha nang luu tru ~32000
 	unsigned short us;//%hu
 	int i; // %d, %i
 	unsigned int ui; //%ui
 	float f; //%f, %F
 	double d; //%lf, %lF
 	long l; //%ld, %li
 	long long ll; //%lli, %lld
 	scanf("%c", &c);
 	printf("gia tri cua bien char = %c", c);
 	scanf("%d", &i);
 	printf("\ngia tri cua bien int = %d", i);
 	return 0;
 	
 }
